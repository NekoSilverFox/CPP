#ifndef ALLSUBJECTS_HPP
#define ALLSUBJECTS_HPP

namespace jianing
{
  void subject1(const char* sort_type);
  void subject2(const char* filename);
  void subject3();
  void subject4(const char* sort_type, const char* array_size);
}

#endif
